# Core deep-learning stack
torch>=2.1
numpy>=1.24

# Atari environment (Gymnasium replaces the unmaintained gym)
gymnasium>=0.29
ale-py>=0.8          # ROMs ship with ale-py>=0.10; for older versions run: AutoROM --accept-license

# Image / plotting / utilities
opencv-python>=4.7
matplotlib>=3.6
tqdm>=4.65
pandas>=2.0

# Spiking network (snn_conversion/ only)
snntorch>=0.7
